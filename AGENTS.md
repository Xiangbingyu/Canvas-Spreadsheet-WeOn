# Agent Instructions

本仓库为 monorepo（`frontend/` + `backend-js/` + `docs/`）。

## 前端任务（必读）

凡涉及 **`frontend/`** 的编写、修改、评审、排错，**必须先阅读并严格遵守**：

→ **[frontend/AGENTS.md](frontend/AGENTS.md)**

其中包含：目录层级、`spreadsheet/` 各模块职责、**AI 红线**、新建文件规则与数据流约定。

## 范围说明

| 路径 | 本规范 |
|------|--------|
| `frontend/**` | ✅ 受 `frontend/AGENTS.md` 约束 |
| `backend-js/**` | ❌ 不在前端规范内 |
| `docs/**` | 接口/需求文档；改接口前先更新文档 |
