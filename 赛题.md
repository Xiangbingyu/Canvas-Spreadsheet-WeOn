本课题要求实现一个 _可演示、可协作、可编辑_ 的在线电子表格：支持上传 Excel → 解析 → 在浏览器中以 **Canvas** 方式渲染成可交互表格；支持单元格文本编辑、工具栏修改样式、undo/redo，以及多人协同编辑。

参考产品：Google Sheet、飞书表格、腾讯表格等

## 项目背景与目标
### 背景
电子表格是典型的 “复杂交互 + 高性能渲染 + 数据一致性” 前端工程题目，能真实考察：

+ 大数据量（行列多）下的渲染与交互性能
+ 复杂状态管理（单元格、样式、选区、滚动、公式/文本）
+ 编辑体验（输入法、光标、选区、复制粘贴）
+ 操作历史（undo/redo）
+ 多人协作（冲突处理、实时同步、离线恢复）

本项目**不要求完全对齐** Excel 全部能力，但要求围绕 “上传→渲染→编辑→样式→撤销重做→协同” 的闭环交付一个可演示 Demo。

### 核心目标
**P0（必须）**：

1. 上传 `.xlsx/.xls`，解析为工作表数据，支持刷新
2. 在 Web 中以 **Canvas** 渲染成在线表格（含滚动/视口）
3. 可编辑输入单元格文本（含中文输入法基本可用）
4. 支持增删行列
5. 工具栏可修改单元格样式（如粗体/斜体/字号/颜色/对齐/背景）
6. 支持 **undo/redo**（至少覆盖：文本修改、样式修改、插入/删除行列）
7. 支持 **协同编辑**（至少 2 人同时编辑同一文档，实时看到对方更改）

**P1（加分）**：

+ 复制/粘贴（与系统剪贴板互通或表格内部互通）
+ 简单公式解析与计算
+ 离线编辑与重连自动同步
+ 性能优化（10k+ 单元格仍可流畅滚动与编辑）

**P2（挑战）**：

+ 简单公式解析与计算
+ 简单对齐 Excel：多选区、合并单元格、冻结行列

## 周期与关键日期（建议）
+ W1：范围冻结 + 技术选型 + MVP0（能渲染）
+ W2：MVP1（可编辑 + 样式工具栏）
+ W3：MVP2（undo/redo + 协同最小链路）
+ W4：打磨（性能、异常、空态、测试、答辩材料）

## 预期受众 & 角色分工（建议）
每组 3–5 人，建议角色：

+ **FE Owner（必选）**：整体架构、核心渲染/编辑链路、性能与最终发布
+ **渲染/交互负责人**：Canvas 渲染、滚动/虚拟化、选区/命中检测
+ **协同负责人（可兼任）**：协议设计、WebSocket/RTC、CRDT/OT、离线重连
+ **工程化/质量负责人（可兼任）**：测试、CI、性能指标、可访问性（若涉及）

## 功能范围（必须项）
### 文档与工作表
+ 支持上传 Excel 文件（`.xlsx` 优先，`.xls` 可选）
+ 支持解析至少 1 个工作表（多 Sheet 为加分）
+ 支持基本单元格数据：
+ `text`（字符串/数字以字符串呈现也可）
+ `style`（字体、字号、颜色、背景、对齐）

### Canvas 表格渲染
+ 以 Canvas 绘制网格、单元格背景、文本
+ 支持滚动（横向/纵向）与视口渲染（虚拟化滚动）
+ 支持选中单元格（高亮边框）

### 单元格编辑
+ 双击或回车进入编辑态
+ 支持输入/删除文本，提交后 Canvas 更新
+ 基本可用的中文输入法（composition 事件不丢字）

### 工具栏：样式修改
至少支持以下任意 5 项，设置后 Canvas 渲染更新（建议全做）：

+ 加粗/斜体/下划线
+ 字号
+ 字体颜色
+ 背景色
+ 水平对齐（左/中/右）

### Undo / Redo
+ 支持撤销/重做
+ 至少覆盖：文本变更、样式变更
+ 支持快捷键：`Ctrl/⌘ + Z`、`Ctrl/⌘ + Shift + Z`（或 `Ctrl/⌘ + Y`）

### 协同编辑
+ 至少支持 2 人加入同一文档房间
+ 双方能实时看到：
+ 对方提交的单元格内容/样式变更
+ 基本的在线状态（谁在房间里）
+ 冲突处理要求：
+ 同一单元格被同时修改时，不允许出现 “客户端各自为政”
+ 允许采用 “后写覆盖” 或 “基于 CRDT/OT 合并” 策略，但必须解释一致性策略

## 建议的实现方式（不强绑技术栈）
+ 框架：React + Vite + TypeScript / Vue3 + Vite + TypeScript
+ Excel 解析：`SheetJS (xlsx)`（允许三方库）
+ 渲染：Canvas 2D（P0），WebGL（P1 加分）
+ 状态管理：Zustand/Redux/Pinia（任选）
+ 协同通信：WebSocket（推荐）
+ 本地持久化：IndexedDB（P1）

## 技术方案（建议写清楚的关键点）
### 总体架构
建议拆成 4 层：

1. **Data Model（表格模型）**：单元格值、样式、行列宽高、合并信息（可选）
2. **Render Engine（渲染引擎）**：Canvas 绘制 + 视口计算 + 缓存
3. **Interaction（交互层）**：命中检测、选区、滚动、编辑态、快捷键
4. **Collab（协同层）**：操作（operation）编码、同步、冲突处理、回放

推荐把 “用户动作” 统一抽象为 Operation，渲染与协同都消费同一套操作流。

### 数据结构设计（示例）
**基本类型**

+ `CellKey = { row: number; col: number }`
+ `Cell = { value: string; styleId?: string }`
+ `Style = { fontFamily; fontSize; bold; italic; underline; color; bgColor; hAlign }`

**存储建议**

+ `cells: Map<string, Cell>`（稀疏存储，key 可用 `r:c`）
+ `styles: Map<string, Style>`（样式去重，styleId 可用 hash）
+ `rowHeights / colWidths: Map<number, number>`（可选）

### Excel 导入
+ 解析：`xlsx.read` → `workbook.Sheets[sheetName]` → `utils.sheet_to_json`/自行遍历单元格
+ 注意点：
+ 数字/日期格式可先按字符串展示（P0 可简化）
+ 样式解析：SheetJS 对样式支持有限，P0 可只导入值；P1 再尝试导入部分样式
+ 大文件：导入需要进度提示与错误处理

### Canvas 渲染与虚拟化
**视口模型**

+ 维护 `scrollX/scrollY`、`viewportWidth/Height`
+ 根据行高列宽（默认常量也可）计算可见行列范围：`[rowStart,rowEnd]`, `[colStart,colEnd]`

**绘制顺序（建议）**

1. 清屏
2. 绘制可见单元格背景（含选中背景）
3. 绘制网格线
4. 绘制文本（注意裁剪，避免越界）
5. 绘制选中边框/填充柄（可选）

**性能策略**

+ 只绘制可见区域（虚拟化）
+ 文本测量缓存（`measureText` 结果缓存）
+ 分层 Canvas（可选）：grid 层、content 层、overlay 层
+ 节流/合并渲染：`requestAnimationFrame` 驱动

### 单元格编辑实现（关键难点）
Canvas 不能直接输入文字，推荐使用 “DOM 输入层覆盖” 的方案：

+ 进入编辑态时，在目标单元格位置 **绝对定位** 一个 `contenteditable` 或 `<textarea>`
+ DOM 输入完成后（blur/enter），提交 value → 更新 data model → 触发重绘

需要处理：

+ composition（中文输入法）期间不要频繁提交
+ 光标定位与滚动联动（单元格滚出视口时自动滚动）
+ ESC 取消、Enter 提交、Tab 切换单元格（可选）

### 样式系统与工具栏
**样式作用范围**

+ P0：只对 “当前选中单元格” 生效
+ P1：支持区域选中（range）批量应用

**样式合并策略（加分：享元引用化）**

+ `cell.styleId` 指向一个 Style
+ 工具栏修改时：基于旧 Style 生成新 Style（或在 styles 中复用已有 styleId）

### Undo / Redo（建议以 Operation 为中心）
**Operation 定义（示例）**

+ `SetCellValueOp { type:'set_cell_value', key, prev, next }`
+ `SetCellStyleOp { type:'set_cell_style', key, prevStyleId, nextStyleId }`

**栈模型**

+ `undoStack: Op[]`
+ `redoStack: Op[]`

规则：

+ 执行新 op：push undoStack，清空 redoStack
+ undo：pop undoStack，执行 `inverse(op)`，push redoStack
+ redo：pop redoStack，重新 apply，push undoStack

加分：支持 “事务/批量操作”（例如批量设置区域样式作为一个 undo 单元）。

### 协同编辑（必须解释一致性策略）
**最小可行协同（推荐路线：中心化房间 + 操作广播）**

+ Client 产生 op → 发送到 Server
+ Server 为 op 分配全局递增序号 `seq`（或时间戳 + tie-breaker）
+ Server 广播给房间其他人
+ Client 按 `seq` 顺序 apply op，保证最终一致

冲突策略（P0 可接受）：

+ **同单元格并发写入：后写覆盖（Last-Write-Wins, LWW）**
+ 需要明确 “后写” 的定义：以 server `seq` 为准

**进阶协同（加分：CRDT/OT）**

+ 若要支持离线编辑与更强一致性，可考虑：
+ 单元格级别 LWW-Register CRDT（值/样式分别一个寄存器）
+ 或 OT：对同一单元格的文本编辑做 transform（较复杂，P1 可选）

**undo/redo 与协同的关系（必须说明）**

常见做法二选一：

1. **本地 undo（仅回滚自己操作）**：undo 产生一个 “反向 op” 并广播
2. **全局 undo（回滚最近全房间操作）**：需要全局历史与权限控制（更复杂）

建议：P0 做 **本地 undo**，即 undo 时生成 `inverse(op)`，作为新 op 广播。

## 接口契约（建议）
课题允许前后端均由你们实现，也允许使用本地 stub。这里给出 WebSocket 房间协议示例。

### WebSocket 事件
+ `join`：加入文档
+ req：`{ docId, clientId, userInfo }`
+ resp：`{ ok, snapshot, seq }`
+ `op`：提交操作
+ req：`{ docId, clientId, baseSeq, op }`
+ broadcast：`{ docId, seq, clientId, op }`
+ `presence`：在线用户
+ broadcast：`{ docId, users: [...] }`

### Snapshot
+ `snapshot` 至少包含：`cells/styles/rowHeights/colWidths`（按你们实现范围）

## 交付物
+ **线上 Demo**：可公开访问（Vercel/Netlify/自部署均可）
+ **代码仓库**：包含以下文档（至少 3 个）
+ `README.md`：如何启动、核心能力、演示链接
+ `ARCHITECTURE.md`：架构图 + 关键链路说明（渲染、编辑、协同、undo）
+ `PROTOCOL.md`（或 `COLLAB.md`）：协同协议与一致性策略
+ `PERF.md`（可选）：性能指标与优化记录
+ **答辩材料**：10–12 分钟演示 PPT（产品动线 + 实机演示 + 架构与风险）

## 时间安排（参考）
| **周期** | **目标** | **验收点** |
| :--- | :--- | :--- |
| W1 | 范围冻结 + 骨架 | 能导入 Excel + Canvas 渲染网格与文本 |
| W2 | 编辑与样式 | 可编辑单元格 + 工具栏改样式 |
| W3 | 历史与协同 | undo/redo 可用 + 2 人协同实时同步 |
| W4 | 打磨 | 性能优化 + 测试/CI + 答辩材料 |


## 评分标准（总分 100）
| **维度** | **分值** | **明确标准** |
| :--- | :--- | :--- |
| 产品理解与范围控制 | 10 | 范围合理、交付闭环清晰、承诺与实现一致 |
| 交互与易用性 | 20 | 选中/编辑流畅、输入法可用、空态/错误提示完善 |
| 工程化 | 15 | TS、模块化、lint、CI 全绿，代码可维护 |
| 渲染性能 | 15 | 滚动/缩放（若有）不卡顿，大数据量仍可用 |
| Undo/Redo 设计 | 15 | 覆盖核心操作，边界清晰，解释为何可用 |
| 协同一致性 | 20 | 多端最终一致，冲突策略明确，协议可落地 |
| 文档与答辩 | 5 | 文档齐全、演示清晰 |


一票否决：Demo 无法运行 / **明显抄袭** / 泄露密钥或敏感信息。

## 加分项（拉开区分度）
+ 支持多 Sheet 切换与导出回 Excel
+ 区域选中 + 批量样式/批量输入
+ 复制粘贴（含跨应用粘贴 TSV/CSV）
+ 离线编辑 + 重连同步（含本地队列与重放）
+ WebGL 渲染或更深度的分层缓存
+ **简单公式（**`=SUM(A1:A10)`**）与依赖更新（高难度）**
+ **项目中运用 AI 提效**

## Bonus 项（体现创新性）
+ 简易设计实现 AI 驱动编辑表格

## 验收清单（答辩前自检）
+ [ ] 支持上传 Excel 并成功渲染到 Canvas
+ [ ] 单元格可编辑输入（含中文输入法基本可用）
+ [ ] 工具栏可修改样式且可见
+ [ ] undo/redo 可用（含快捷键）
+ [ ] 至少 2 人协同编辑同一文档，变更能实时同步
+ [ ] 关键异常/空态：上传失败、解析失败、空文件、网络断开
+ [ ] 提供性能数据（至少 1 个场景：例如 100x100 / 500x200 单元格滚动）
+ [ ] CI 通过截图或链接

## 风险与替代方案
+ **Excel 样式解析困难**：P0 只导入值；样式用前端自定义体系
+ **Canvas 输入体验差**：必须用 DOM overlay；复杂光标/选区可先不做
+ **协同一致性复杂**：P0 用 server seq + LWW；文档中明确限制与后续可升级 CRDT
+ **性能不达标**：优先做虚拟化 + rAF 合并渲染 + 文本测量缓存
+ **后端不可用**：提供本地 stub（模拟房间、广播、snapshot），保证演示闭环

## 评审当日演示脚本（建议）
1. 背景与目标（30s）
2. 核心演示（2–3min）：上传 Excel → 渲染 → 编辑 → 改样式 → undo/redo
3. 协同演示（2min）：两端同时操作，展示一致性与冲突策略
4. 架构讲解（3min）：数据模型、渲染引擎、编辑 overlay、操作历史、协同协议
5. 性能与复盘（2min）：指标、优化点、风险与下一步

## 发布与协作说明
+ 交付以 “可运行 Demo + 可读文档” 为准
+ 不等后端、不等完美：先闭环，再升级
+ 建议每周至少 1 次可演示版本（带 Tag/Release）
+ **可以使用 AI 参与编码与设计**
+ **可参考开源架构设计，****<font style="background-color:rgb(247,105,100);">禁止</font>****直接魔改开源库交付**
