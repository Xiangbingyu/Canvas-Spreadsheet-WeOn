# 数据流

1. 文档列表页。点击【打开】\- navigate 路由跳转至表格页 \- 挂载 `SpreadsheetPage` 组件 \- 分配 `userId`【若 `localStorage` 有 id，直接用】 \- 解析 url `docId`，请求 `GET /docs/:docId` \- `const doc = await API.getDoc(routeDocId)`，当前 `doc.snapshot` 已经是 **workbook 结构** \- 更新 `workbookStore`，存所有 sheet；更新 `worksheetStore`，切到 `activeSheetId` 对应 sheet；`setDocSession` 保存 `docId` 和 `userId` \- 挂载 `SpreadsheetWorkspace` \- 从 redux 拿 `docId` 和 `userId` 发起 `join`；Canvas 从当前 `worksheetStore` 重绘

2. 分享协作链接进入。点击【链接】 \- 路由判断，挂载SpreadsheetPage组件 \- 其余与上述相同1

3. 文档列表页。点击【新建空白文档】 \- `POST /docs` 请求【当前支持 `eventId` 幂等】 \- 服务端返回默认 workbook（包含 1 个空白 `Sheet1`） \- navigate 路由跳转至表格页 \- 后续同 1

4. 表格页。点击【新建空白文档】 \- `POST /docs` 请求【当前支持 `eventId` 幂等】 \- navigate 路由跳转【同路由下，触发组件重渲染】 \- 解析 url `docId` \- `routeDocId` 变化后执行 `useEffect` 副作用，重新请求 `GET /docs/:docId` \- 之后同 1

5. 导入 Excel 表【当前按整份 workbook 导入】\- 前端解析 Excel，生成 workbook 结构 \- 发起 `import_sheet` WebSocket 请求 \- 服务端整体更新 `doc.snapshot_json`，追加 `history`，并清空当前用户的 `undoStack/redoStack` \- 返回 `sheet_imported`，并广播给同房间用户 \- 前端据返回的 workbook 更新 `workbookStore` 和当前 `worksheetStore`

6. 编辑单元格。前端基于当前激活 tab 显式带上 `sheetId`，发送 `set_cell` \- 服务端按 `sheetId + row + col` 修改目标 sheet \- 返回并广播 `cell_updated` \- 前端据 `sheetId` 更新对应 sheet，不依赖全局 `activeSheetId` 推断

7. 新增工作表。前端发起 `add_sheet` WebSocket 请求，显式携带 `docId`、`clientId`，可选带 `sheetName` \- 服务端生成新的 `sheetId`，创建空白 sheet，追加到 `sheetOrder`，并将 `activeSheetId` 切到新 sheet \- 返回并广播 `sheet_added` \- 前端把新 `sheet` 写入 `workbookStore`，并切换当前 `worksheetStore`




**工作薄结构**

```JSON
{
  "activeSheetId": "sheet_20260527_001",
  "sheetOrder": ["sheet_20260527_001", "sheet_20260527_002"],
  "sheets": {
    "sheet_20260527_001": { /* worksheet snapshot */ },
    "sheet_20260527_002": { /* worksheet snapshot */ }
  }
}
```

说明：

- `activeSheetId` 表示当前文档默认激活的工作表，不代表每个用户各自实时正在查看的 tab
- `sheetOrder` 表示工作表顺序
- `sheets` 是以 `sheetId` 为 key 的工作表字典









### **GET /docs/:docId — 查询单个文档**

请求，携带文档ID

响应 `data`（外层不变，只改 snapshot）

```JSON
{
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "title": "2026年销售数据表",
    "currentSeq": 12,
    "createdBy": "system",
    "createdAt": "2026-05-27T10:00:00.000Z",
    "updatedAt": "2026-05-27T10:00:00.000Z",
    "snapshot": {
      "activeSheetId": "sheet_20260527_001",
      "sheetOrder": [
        "sheet_20260527_001",
        "sheet_20260527_002",
        "sheet_20260527_003"
      ],
      "sheets": {
        "sheet_20260527_001": {
          "id": "sheet_20260527_001",
          "name": "2026年销售数据表",
          "defaultRowHeight": 25,
          "defaultColWidth": 100,
          "rowCount": 100,
          "colCount": 26,
          "styles": { "style_header": { "fontFamily": "微软雅黑", "fontSize": 14, "bold": true } },
          "cells": {
            "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
            "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
            "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
          }
        },
        "sheet_20260527_002": {
          "id": "sheet_20260527_002",
          "name": "汇总",
          "defaultRowHeight": 25,
          "defaultColWidth": 110,
          "rowCount": 20,
          "colCount": 8,
          "styles": {
            "style_header": { "fontFamily": "微软雅黑", "fontSize": 14, "bold": true },
            "style_currency": { "fontFamily": "Arial", "fontSize": 12, "bold": false },
            "style_status": { "fontFamily": "微软雅黑", "fontSize": 12, "bold": true }
          },
          "cells": {
            "0:0": { "row": 0, "col": 0, "value": "指标", "styleId": "style_header" },
            "0:1": { "row": 0, "col": 1, "value": "数值", "styleId": "style_header" },
            "1:0": { "row": 1, "col": 0, "value": "总销售额", "styleId": null },
            "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
          }
        },
        "sheet_20260527_003": {
          "id": "sheet_20260527_003",
          "name": "区域明细",
          "defaultRowHeight": 25,
          "defaultColWidth": 100,
          "rowCount": 30,
          "colCount": 10,
          "styles": {},
          "cells": {
            "0:0": { "row": 0, "col": 0, "value": "区域", "styleId": "style_header" },
            "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
            "1:0": { "row": 1, "col": 0, "value": "华东", "styleId": null },
            "1:1": { "row": 1, "col": 1, "value": "4200.00", "styleId": "style_currency" }
          }
        }
      }
    }
  }
}
```

### POST /docs — 创建文档

请求不变

响应 `data`（外层不变，只改 snapshot）

```JSON
"snapshot": {
  "activeSheetId": "sheet_doc_001_001",
  "sheetOrder": ["sheet_doc_001_001"],
  "sheets": {
    "sheet_doc_001_001": {
      "id": "sheet_doc_001_001",
      "name": "Sheet1",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 0,
      "colCount": 0,
      "styles": {},
      "cells": {}
    }
  }
}
```

### **join — 加入文档房间**

请求（不变）

**响应一 — \`join\_ack\`，snapshot快照变了**

```JSON
{
  "type": "join_ack",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "currentSeq": 12,
    "snapshot": { "activeSheetId": "...", "sheetOrder": [...], "sheets": { ... } },
    "users": [ ... ]
  }
}
```

**响应二 — \`presence\`不变**





### set\_cell — 更新单元格

请求（新增 `sheetId`）。

说明：

- 前端应显式传当前编辑目标的 `sheetId`
- 后端当前已收紧为：`set_cell` 必须显式传 `sheetId`
- 如果 `sheetId` 不存在，服务端返回参数错误
- 若发送方未先 `join` 当前文档，返回 `4003`



```JSON
{
  "type": "set_cell",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetId": "sheet_20260527_001",
  "row": 1,
  "col": 1,
  "value": "hello"
}
```

响应 cell\_updated（reply \+ broadcast，新增 sheetId）

```JSON
{
  "type": "cell_updated",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "row": 1,
    "col": 1,
    "value": "hello",
    "style": null,
    "seq": 13,
    "canUndo": true,
    "canRedo": false
  }
}
```

### set\_title — 更新文档标题

请求（不涉及 workbook 内部 sheet 结构，只修改 doc 级 title）

```JSON
{
  "type": "set_title",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "title": "2026年销售数据表（已更新）"
}
```

响应 `title_updated`（reply + broadcast，结构保持 doc 级）

```JSON
{
  "type": "title_updated",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 14,
    "title": "2026年销售数据表（已更新）"
  }
}
```

### import\_sheet — 整表导入

请求（snapshot 形状变更）

```JSON
{
  "type": "import_sheet",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "snapshot": {
    "activeSheetId": "sheet_import_001",
    "sheetOrder": ["sheet_import_001"],
    "sheets": { ... }
  }
}
```

响应 sheet\_imported

```JSON
{
  "type": "sheet_imported",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 14,
    "snapshot": { "activeSheetId": "...", "sheetOrder": [...], "sheets": { ... } }, //这个变了
    "canUndo": false,
    "canRedo": false
  }
}
```

### undo — 撤销

请求（不变）

响应 `undo_applied`（新增 `sheetId`，需要知道是哪张表做了操作）

```JSON
{
  "type": "undo_applied",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "row": 1,
    "col": 1,
    "value": "before-value",
    "style": null,
    "seq": 15,
    "canUndo": false,
    "canRedo": true
  }
}
```


### redo — 重做

请求（不变）：`type, docId, clientId`
响应 `redo_applied`（同 `undo_applied`，新增 `sheetId`）

```JSON
{
  "type": "redo_applied",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "row": 1,
    "col": 1,
    "value": "after-value",
    "style": null,
    "seq": 16,
    "canUndo": true,
    "canRedo": false
  }
}
```
响应 redo\_applied（同 undo\_applied，新增 sheetId）


### `add_sheet` — 新建工作表

```JSON
{
  "type": "add_sheet",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetName": "销售明细"
}
```

响应 — `sheet\_added`（reply \+ broadcast）

```JSON
{
  "type": "sheet_added",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 15,
    "activeSheetId": "sheet_doc_sys_001_004",
    "sheet": {
      "id": "sheet_doc_sys_001_004",
      "name": "销售明细",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 0,
      "colCount": 0,
      "styles": {},
      "cells": {}
    },
    "sheetOrder": [
      "sheet_20260527_001",
      "sheet_20260527_002",
      "sheet_20260527_003",
      "sheet_doc_sys_001_004"
    ]
  }
}
```

说明：

- `sheetId` 由服务端生成，当前默认采用 `sheet_${docId}_序号` 形式
- `add_sheet` 成功后会把 `activeSheetId` 切到新建的 sheet
- 当前 `add_sheet` 会写入 `history`，但不会进入用户 `undoStack/redoStack`

## 当前已完成

- `GET /docs/:docId` 返回 workbook 结构
- `POST /docs` 返回默认 workbook
- `join_ack` 返回 workbook 结构
- `set_cell` 请求已支持 `sheetId`，响应 `cell_updated` 已返回 `sheetId`
- `import_sheet` 请求 / 响应已切为 workbook 结构
- `add_sheet` / `sheet_added` 已支持新增空白工作表
- `undo_applied` / `redo_applied` 已返回 `sheetId`
- `history` 已增加 `target_sheet_id`
- `undoStack/redoStack` entry 已携带 `sheetId`

## 当前待完成

- 评估是否需要 `rename_sheet` / `delete_sheet`
- 明确并统一 0-based / 1-based 单元格坐标规范
- 清理其余测试和文档里仍残留的单 sheet 断言 / 示例
