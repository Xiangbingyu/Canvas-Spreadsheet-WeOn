// Compatibility exports for render chrome constants and column labels.
// Input: render callers; output: shared coordinate helpers from spreadsheet/utils.

export { GRID_CHROME, colNumberToLetters } from '@/spreadsheet/utils/coordinates'
