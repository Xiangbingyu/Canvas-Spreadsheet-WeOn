# 前端环境变量（复制为 .env 后生效：cp .env.example .env）
#
# 本地联调（前后端都在本机、pnpm dev）：
#   两项留空，HTTP/WS 走 Vite proxy → localhost:3000
#
# 远程后端（例如 api-test 部署在 120.48.16.108:3000）：
#   填写下方地址；需重启 pnpm dev:fe

# HTTP API 根地址（不要末尾斜杠）
VITE_API_BASE_URL=

# WebSocket 根地址（与 api-test 页一致，无 /ws 路径）
VITE_WS_URL=
