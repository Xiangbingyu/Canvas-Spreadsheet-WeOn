# 后端接口文档
- 版本： v1（当前实现）
- HTTP 基础地址： `http://localhost:3000`
- WebSocket 地址： `ws://localhost:3000`

---

## 目录

- [通用约定](#通用约定)
- [业务码对照表](#业务码对照表)
- [HTTP 接口](#http-接口)
- [GET / — 服务根路由](#get---服务根路由)
- [GET /health — 健康检查](#get-health--健康检查)
- [POST /docs — 创建文档](#post-docs--创建文档)
- [GET /docs — 查询文档列表](#get-docs--查询文档列表)
- [GET /docs/:docId — 查询单个文档](#get-docsdocid--查询单个文档)
- [WebSocket 接口](#websocket-接口)
- [连接说明](#连接说明)
- [消息通用格式](#消息通用格式)
- [join — 加入文档房间](#join--加入文档房间)
- [presence — 查询在线用户](#presence--查询在线用户)
- [set_cell — 更新单元格](#set_cell--更新单元格)
- [import_sheet — 整表导入](#import_sheet--整表导入)
- [undo — 撤销操作](#undo--撤销操作)
- [redo — 重做操作](#redo--重做操作)
- [错误消息](#错误消息)
- [附录](#附录)
- [快照结构](#快照结构)
- [用户对象](#用户对象)
- [种子数据](#种子数据)

---

## 通用约定

#### HTTP

- 有请求体的请求需携带 Content-Type: application/json 请求头（GET 请求无需携带）。
- HTTP 状态码遵循标准语义：200 成功、201 创建成功、400 参数错误、404 资源不存在、500 服务器错误。
- 正常路由处理的响应体统一为以下 JSON 结构。例外：若请求携带了格式非法的 JSON 正文（无法解析），Express 内置的 body-parser 会在路由执行前直接返回 `400 text/html` 错误页，不经过业务逻辑，也不符合此统一结构。

```json
{
  "code": 0,
  "message": "ok",
  "data": { }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| code | number | 业务码，0 表示成功，非零表示错误 |
| message | string | 说明文字，成功时为 "ok"，错误时为具体描述 |
| data | object \| null | 业务数据，错误时为 null |

> 发送格式非法的 JSON 正文（如 `POST /docs` 请求体不是合法 JSON），服务端返回 `400 text/html` Express 错误页，而非上述 JSON 结构。前端应避免发送格式破损的请求体。

#### WebSocket

- 所有消息均为 JSON 字符串，必须包含 type 字段。
- 服务端响应同样包含 type、code、message、data 字段。
- 单条消息最大负载为 1 MB（由 `WS_MAX_PAYLOAD_BYTES` 环境变量控制）。

---

## 业务码对照表

| 业务码 | 对应 HTTP 状态码 | 含义 |
| --- | --- | --- |
| 0 | 200 / 201 | 成功 |
| 4000 | 400 | 参数错误 / 业务规则不满足 |
| 4001 | 400 | 不支持的消息类型（WebSocket 专用） |
| 4003 | 403 | 无权限（WebSocket 专用，如未 join 直接调用需要房间成员资格的接口） |
| 4004 | 404 | 文档不存在 |
| 5000 | 500 | 服务器内部错误 |

---

## HTTP 接口

### GET / — 服务根路由

确认服务身份。

#### 完整请求示例

```http
GET http://localhost:3000/
```

#### 响应 — HTTP `200`

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "service": "backend-js"
  }
}
```

---

### GET /health — 健康检查

确认服务是否正常运行。

#### 完整请求示例

```http
GET http://localhost:3000/health
```

#### 响应 — HTTP `200`

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "status": "ok"
  }
}
```

---

### POST /docs — 创建文档

创建一个新的在线文档，返回文档初始状态（含空快照）。

支持通过 eventId 实现幂等控制：携带相同 eventId 的重试请求直接返回首次结果，不重复创建文档。

#### 完整请求示例

```http
POST http://localhost:3000/docs
Content-Type: application/json
```

```json
{
  "title": "季度报表",
  "createdBy": "user_001",
  "eventId": "evt_create_doc_001"
}
```

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 否 | 文档标题；不传时默认 "Untitled"；不可为纯空白字符串；非字符串返回 4000 |
| `createdBy` | string | 否 | 创建人标识；不传或传 `null` 时记录为 `null`；不可为空字符串或纯空白字符串；非字符串返回 `4000` |
| eventId | string | 否 | 幂等键；不传或传 null 时不做幂等控制；不可为纯空白字符串；非字符串返回 4000 |

#### 响应 — HTTP `201`

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_001",
    "title": "季度报表",
    "currentSeq": 0,
    "createdBy": "user_001",
    "createdAt": "2026-05-27T10:00:00.000Z",
    "updatedAt": "2026-05-27T10:00:00.000Z",
    "snapshot": {
      "id": "sheet_doc_001_001",
      "name": "Sheet1",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 0,
      "colCount": 0,
      "styles": {},
      "cells": {}
    }
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档唯一 ID，格式 doc_xxx，自动分配，从 doc_001 起（种子数据 doc_sys_001 使用固定 ID 直接插入，不占用自增计数器） |
| title | string | 文档标题 |
| currentSeq | number | 当前操作序列号，新建文档初始为 0 |
| createdBy | string \| null | 创建人 |
| createdAt | string | 创建时间，ISO 8601 格式 |
| updatedAt | string | 最后更新时间，ISO 8601 格式 |
| snapshot | object | 文档快照，新建文档为空快照，见[快照结构](#快照结构) |

#### 错误响应示例

title 为纯空白字符串 — HTTP 400：

```json
{
  "code": 4000,
  "message": "title must be a non-empty string",
  "data": null
}
```

createdBy 为空字符串或纯空白字符串 — HTTP 400：

```json
{
  "code": 4000,
  "message": "createdBy must be a non-empty string",
  "data": null
}
```

createdBy 类型不是字符串（如传数字）— HTTP 400：

```json
{
  "code": 4000,
  "message": "createdBy must be a string",
  "data": null
}
```

eventId 为纯空白字符串 — HTTP 400：

```json
{
  "code": 4000,
  "message": "eventId must be a non-empty string",
  "data": null
}
```

title 类型不是字符串（如传数字）— HTTP 400：

```json
{
  "code": 4000,
  "message": "title must be a string",
  "data": null
}
```

请求体非 JSON 对象（如传数组）— HTTP 400：

```json
{
  "code": 4000,
  "message": "request body must be an object",
  "data": null
}
```

---

### GET /docs — 查询文档列表

按用户和范围分页查询文档列表，结果按 updatedAt 倒序排列（时间相同时再按 docId 倒序）。

#### 完整请求示例

```http
GET http://localhost:3000/docs?userId=user_001&scope=all&page=1&pageSize=20
```

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| userId | string | 是 | 用户 ID，不可为空 |
| scope | string | 是 | 查询范围：created（我创建的）、participated（我参与的）、all（两者合集） |
| page | number | 是 | 页码，正整数，从 1 开始 |
| pageSize | number | 是 | 每页条数，正整数，最大 100；也兼容写作 pagesize（小写） |

**`scope` 说明**

- created：返回 createdBy === userId 的文档。
- participated：返回该用户通过 WebSocket join 加入过（有 roomUser 记录）、且不是自己创建的文档。
- all：合并以上两类，同一文档只出现一次，relation 优先标记为 "created"。

#### 响应 — HTTP `200`

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "list": [
      {
        "docId": "doc_003",
        "title": "协作文档",
        "createdBy": "user_002",
        "createdAt": "2026-05-27T09:00:00.000Z",
        "updatedAt": "2026-05-27T11:00:00.000Z",
        "currentSeq": 5,
        "relation": "participated"
      },
      {
        "docId": "doc_002",
        "title": "季度报表",
        "createdBy": "user_001",
        "createdAt": "2026-05-27T10:00:00.000Z",
        "updatedAt": "2026-05-27T10:30:00.000Z",
        "currentSeq": 2,
        "relation": "created"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 2,
    "hasMore": false
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| list | array | 当前页文档列表 |
| list[].docId | string | 文档 ID |
| list[].title | string | 文档标题 |
| list[].createdBy | string \| null | 创建人 |
| list[].createdAt | string | 创建时间 |
| list[].updatedAt | string | 最后更新时间 |
| list[].currentSeq | number | 当前序列号 |
| list[].relation | string | 与查询用户的关系："created" 或 "participated" |
| page | number | 当前页码 |
| pageSize | number | 每页条数 |
| total | number | 符合条件的文档总数 |
| hasMore | boolean | 是否还有下一页 |

#### 错误响应示例

userId 为空 — HTTP 400：

```json
{
  "code": 4000,
  "message": "userId must be a non-empty string",
  "data": null
}
```

scope 不合法 — HTTP 400：

```json
{
  "code": 4000,
  "message": "scope must be one of: created, participated, all",
  "data": null
}
```

page 不是正整数（如 0 或负数）— HTTP 400：

```json
{
  "code": 4000,
  "message": "page must be a positive integer",
  "data": null
}
```

pageSize 超过 100 — HTTP 400：

```json
{
  "code": 4000,
  "message": "pageSize must be less than or equal to 100",
  "data": null
}
```

---

### GET /docs/:docId — 查询单个文档

获取文档当前完整状态，包含最新快照。结果优先从缓存读取，缓存未命中时查询 store 并回填缓存。

#### 完整请求示例

```http
GET http://localhost:3000/docs/doc_sys_001
```

#### 路径参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| docId | string | 是 | 文档 ID |

#### 响应 — HTTP `200`

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "title": "2026年销售数据表",
    "currentSeq": 0,
    "createdBy": "system",
    "createdAt": "2026-05-27T10:00:00.000Z",
    "updatedAt": "2026-05-27T10:00:00.000Z",
    "snapshot": {
      "id": "sheet_20260527_001",
      "name": "2026年销售数据表",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 100,
      "colCount": 26,
      "styles": {
        "style_header": {
          "fontFamily": "微软雅黑",
          "fontSize": 14,
          "bold": true,
          "color": "#FFFFFF",
          "bgColor": "#4472C4",
          "hAlign": "center"
        },
        "style_currency": {
          "fontFamily": "Arial",
          "fontSize": 12,
          "bold": false,
          "hAlign": "right"
        }
      },
      "cells": {
        "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
        "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
        "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
      }
    }
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| title | string | 文档标题 |
| currentSeq | number | 当前操作序列号 |
| createdBy | string \| null | 创建人 |
| createdAt | string | 创建时间，ISO 8601 格式 |
| updatedAt | string | 最后更新时间，ISO 8601 格式 |
| snapshot | object | 文档当前全量快照，见[快照结构](#快照结构) |

#### 错误响应示例

文档不存在 — HTTP 404：

```json
{
  "code": 4004,
  "message": "document not found: doc_999",
  "data": null
}
```

---

## WebSocket 接口

### 连接说明

```http
ws://localhost:3000
```

- 建立连接后即可发送消息，无需鉴权握手。
- 每条消息都是一个 JSON 字符串，必须包含 type 字段。
- 客户端断开连接时，服务端自动将其从房间移除，并向房间内其余客户端广播更新后的在线用户列表（presence）。
- 同一 clientId 可以建立多个 socket 连接；只有该 clientId 的最后一个连接断开时，才会触发广播。
- **`join` 与写操作的关系**：当前实现中 `set_cell`、`import_sheet` 不要求发送方先 `join`；未加入房间的 socket 也可以直接执行写入并收到响应。但 `undo`、`redo` 要求发送方已通过 `join` 进入目标文档，且当前 socket 绑定的 `clientId` 必须与消息中的 `clientId` 一致，否则返回 `4003`。所有写操作的广播仍只会发送给已在房间内的客户端。

### 消息通用格式

#### 客户端 → 服务端

```json
{
  "type": "消息类型",
  "...": "其他字段"
}
```

#### 服务端 → 客户端（成功）

```json
{
  "type": "响应类型",
  "code": 0,
  "message": "ok",
  "data": { }
}
```

#### 服务端 → 客户端（错误）

```json
{
  "type": "error",
  "code": 4000,
  "message": "错误描述",
  "data": null
}
```

---

### join — 加入文档房间

加入指定文档的协作房间。

成功后：
- 向当前客户端返回 `join_ack`，包含文档快照和当前在线用户列表。
- 若该 `clientId` 之前不在线，向房间内所有客户端（含自己）广播 `presence`。
- 若该 clientId 已有其他连接在线，则不广播 presence。

**提交后失败策略**：当前实现以 `joinRoom()` 成功作为 join 已提交的边界。一旦加入房间成功，后续在线列表查询、`presence` 广播或审计日志写入即使失败，也不会把本次 `join` 反向改成 `error`；客户端仍以已收到的 `join_ack` 为准。

**并发重复 join（同一连接、同一 docId + clientId）**：若同一 socket 在首条 join 尚未完成时发出第二条相同 join，第二条会等待首条完成并重放相同响应（`join_ack` 或 error），不会触发第二次 `joinRoom` 也不会产生额外 `presence` 广播。

#### 完整发送示例

```json
{
  "type": "join",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "name": "Alice",
  "color": "#ff0000"
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "join" |
| docId | string | 是 | 要加入的文档 ID，必须是非空字符串，否则返回 4000 |
| clientId | string | 是 | 客户端/用户唯一标识，必须是非空字符串，否则返回 4000 |
| name | string | 否 | 显示名称；不传或非字符串时均回退为 ""，不做格式校验 |
| color | string | 否 | 用户标识色；不传、空字符串或非字符串时均回退为 "#3b82f6"，不校验颜色格式 |

**响应一 — `join_ack`（仅返回给当前客户端）**

```json
{
  "type": "join_ack",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "currentSeq": 0,
    "snapshot": {
      "id": "sheet_20260527_001",
      "name": "2026年销售数据表",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 100,
      "colCount": 26,
      "styles": {
        "style_header": {
          "fontFamily": "微软雅黑",
          "fontSize": 14,
          "bold": true,
          "color": "#FFFFFF",
          "bgColor": "#4472C4",
          "hAlign": "center"
        },
        "style_currency": {
          "fontFamily": "Arial",
          "fontSize": 12,
          "bold": false,
          "hAlign": "right"
        }
      },
      "cells": {
        "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
        "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
        "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
      }
    },
    "users": [
      {
        "id": 1,
        "docId": "doc_sys_001",
        "clientId": "user_001",
        "name": "Alice",
        "color": "#ff0000",
        "status": "online",
        "joinedAt": "2026-05-27T10:00:00.000Z",
        "lastActiveAt": "2026-05-27T10:00:00.000Z"
      }
    ]
  }
}
```

**响应二 — `presence`（广播给房间内所有客户端）**

```json
{
  "type": "presence",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "users": [
      {
        "id": 1,
        "docId": "doc_sys_001",
        "clientId": "user_001",
        "name": "Alice",
        "color": "#ff0000",
        "status": "online",
        "joinedAt": "2026-05-27T10:00:00.000Z",
        "lastActiveAt": "2026-05-27T10:00:00.000Z"
      }
    ]
  }
}
```

**`join_ack` data 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 当前客户端 ID |
| currentSeq | number | 当前文档序列号 |
| snapshot | object | 文档当前全量快照，见[快照结构](#快照结构) |
| users | array | 房间内所有在线用户，见[用户对象](#用户对象) |

#### 错误响应示例

缺少 docId 或 clientId：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId and clientId are required",
  "data": null
}
```

文档不存在：

```json
{
  "type": "error",
  "code": 4004,
  "message": "document not found: doc_999",
  "data": null
}
```

---

### presence — 查询在线用户

主动拉取指定文档的当前在线用户列表。服务端先通过 `reply` 向发送方返回一条 `presence`，再通过 `broadcastToRoom` 向房间内所有客户端（含发送方）再发一条。**发送方因此会收到 2 条相同的 `presence` 消息**，其他客户端收到 1 条。

> **需要先 join**：发送方必须已通过 `join` 加入目标文档，否则返回 `4003`。这意味着对未 join 的 `docId` 发送 `presence` 请求也会被拒绝，而非返回空列表。

#### 完整发送示例

```json
{
  "type": "presence",
  "docId": "doc_sys_001"
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "presence" |
| docId | string | 是 | 文档 ID，必须是非空字符串 |

**响应 — `presence`（广播给房间内所有客户端；发送方额外通过 `reply` 多收 1 条，共收到 2 条）**

```json
{
  "type": "presence",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "users": [
      {
        "id": 1,
        "docId": "doc_sys_001",
        "clientId": "user_001",
        "name": "Alice",
        "color": "#ff0000",
        "status": "online",
        "joinedAt": "2026-05-27T10:00:00.000Z",
        "lastActiveAt": "2026-05-27T10:00:00.000Z"
      },
      {
        "id": 2,
        "docId": "doc_sys_001",
        "clientId": "user_002",
        "name": "Bob",
        "color": "#00ff00",
        "status": "online",
        "joinedAt": "2026-05-27T10:01:00.000Z",
        "lastActiveAt": "2026-05-27T10:01:00.000Z"
      }
    ]
  }
}
```

错误响应示例

缺少 docId 或 docId 非字符串：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId is required",
  "data": null
}
```

未 join 该文档（包括 docId 不存在的情况）：

```json
{
  "type": "error",
  "code": 4003,
  "message": "socket has not joined this document",
  "data": null
}
```

---

### set_cell — 更新单元格

更新文档中指定单元格的值和样式。操作成功后：
- 先通过 `reply` 向发送方返回 `cell_updated`，再通过 `broadcastToRoom` 向房间内所有客户端（含发送方）广播同一条消息。**发送方会收到 2 条相同的 `cell_updated`**，其他客户端收到 1 条。
- 将本次操作写入操作历史（historyStore）。
- 将本次操作压入当前用户的 undo 栈，并清空 redo 栈。

> `style` 为全量替换：每次调用需传入该单元格完整的 style 对象，不做字段合并。

#### 完整发送示例

```json
{
  "type": "set_cell",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "row": 1,
  "col": 1,
  "value": "姓名",
  "style": {
    "bold": true,
    "fontSize": 14,
    "color": "#333333"
  }
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "set_cell" |
| docId | string | 是 | 文档 ID，必须是非空字符串，否则返回 4000 |
| clientId | string | 是 | 操作用户 ID，必须是非空字符串，否则返回 4000 |
| row | number | 是 | 行号，正整数，从 1 开始 |
| col | number | 是 | 列号，正整数，从 1 开始 |
| `value` | string | 否 | 单元格文本值；不传时视为 `""`。当前不做类型校验，非字符串值会被原样存储并广播 |
| `style` | object | 否 | 单元格样式；不传时视为 `null`（清空样式）。当前不做类型校验，非对象值会被原样存储并广播 |

**`style` 字段说明**

后端对 `style` 对象不做白名单校验，任何字段均会原样存储并广播给房间内其他客户端。以下是前端约定使用的字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| bold | boolean | 是否加粗 |
| italic | boolean | 是否斜体 |
| underline | boolean | 是否下划线 |
| fontSize | number | 字体大小（px） |
| align | string | 对齐方式："left" / "center" / "right" |
| color | string | 字体颜色，如 "#ff0000" |
| backgroundColor | string | 背景颜色，如 "#ffffff" |

**响应 — `cell_updated`（`broadcastToRoom` 广播给房间内所有客户端；发送方额外通过 `reply` 多收 1 条，共收到 2 条）**

```json
{
  "type": "cell_updated",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 1,
    "row": 1,
    "col": 1,
    "value": "姓名",
    "style": {
      "bold": true,
      "fontSize": 14,
      "color": "#333333"
    },
    "canUndo": true,
    "canRedo": false
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| seq | number | 本次操作的序列号，全文档单调递增 |
| row | number | 行号 |
| col | number | 列号 |
| value | string | 写入后的单元格值 |
| style | object \| null | 写入后的单元格样式 |
| canUndo | boolean | 操作后当前用户的 undo 栈是否非空（set_cell 后恒为 true） |
| canRedo | boolean | 操作后当前用户的 redo 栈是否非空（set_cell 后恒为 false，因为 redo 栈已被清空） |

#### 错误响应示例

缺少必填字段或 row/col 不是正整数：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId, clientId, row and col are required",
  "data": null
}
```

文档不存在：

```json
{
  "type": "error",
  "code": 4004,
  "message": "document not found: doc_999",
  "data": null
}
```

---

### import_sheet — 整表导入

用传入的快照全量覆盖文档内容。操作成功后：
- 先通过 `reply` 向发送方返回 `sheet_imported`，再通过 `broadcastToRoom` 向房间内所有客户端（含发送方）广播同一条消息。**发送方会收到 2 条相同的 `sheet_imported`**，其他客户端收到 1 条。
- 将本次操作写入操作历史（historyStore）。
- 清空当前用户的 undo 栈和 redo 栈。

#### 完整发送示例

```json
{
  "type": "import_sheet",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "snapshot": {
    "id": "sheet_20260527_001",
    "name": "2026年销售数据表",
    "defaultRowHeight": 25,
    "defaultColWidth": 100,
    "rowCount": 100,
    "colCount": 26,
    "styles": {
      "style_header": {
        "fontFamily": "微软雅黑",
        "fontSize": 14,
        "bold": true,
        "color": "#FFFFFF",
        "bgColor": "#4472C4",
        "hAlign": "center"
      },
      "style_currency": {
        "fontFamily": "Arial",
        "fontSize": 12,
        "bold": false,
        "hAlign": "right"
      }
    },
    "cells": {
      "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
      "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
      "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
    }
  }
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "import_sheet" |
| docId | string | 是 | 文档 ID，必须是非空字符串，否则返回 4000 |
| clientId | string | 是 | 操作用户 ID，必须是非空字符串，否则返回 4000 |
| `snapshot` | object | 是 | 要导入的全量快照，见[快照结构](#快照结构)。**当前仅校验 `snapshot` 是非数组对象，不校验内部字段类型**：`cells`/`styles` 若为非对象标量会被原样存储，`rowCount`/`colCount` 若为非整数会被归零 |

**响应 — `sheet_imported`（`broadcastToRoom` 广播给房间内所有客户端；发送方额外通过 `reply` 多收 1 条，共收到 2 条）**

```json
{
  "type": "sheet_imported",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 3,
    "snapshot": {
      "id": "sheet_20260527_001",
      "name": "2026年销售数据表",
      "defaultRowHeight": 25,
      "defaultColWidth": 100,
      "rowCount": 100,
      "colCount": 26,
      "styles": {
        "style_header": {
          "fontFamily": "微软雅黑",
          "fontSize": 14,
          "bold": true,
          "color": "#FFFFFF",
          "bgColor": "#4472C4",
          "hAlign": "center"
        },
        "style_currency": {
          "fontFamily": "Arial",
          "fontSize": 12,
          "bold": false,
          "hAlign": "right"
        }
      },
      "cells": {
        "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
        "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
        "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
      }
    },
    "canUndo": false,
    "canRedo": false
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| seq | number | 本次操作的序列号 |
| snapshot | object | 导入后的最新全量快照，见[快照结构](#快照结构) |
| canUndo | boolean | 导入后 undo 栈已清空，恒为 false |
| canRedo | boolean | 导入后 redo 栈已清空，恒为 false |

错误响应示例

缺少 snapshot 或 snapshot 不是对象：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId, clientId and snapshot are required",
  "data": null
}
```

文档不存在：

```json
{
  "type": "error",
  "code": 4004,
  "message": "document not found: doc_999",
  "data": null
}
```

---

### undo — 撤销操作

撤销当前用户在指定文档上最近一次可撤销操作（仅撤销自己的操作，不影响其他用户）。发送方必须已通过 `join` 加入该文档，且当前 socket 绑定的 `clientId` 必须与消息中的 `clientId` 一致，否则返回 `4003`。操作成功后：
- 先通过 `reply` 向发送方返回 `undo_applied`，再通过 `broadcastToRoom` 向房间内所有客户端（含发送方）广播同一条消息。**发送方会收到 2 条相同的 `undo_applied`**，其他客户端收到 1 条。
- 写入操作历史（opType: `"undo"`，sourceSeq 指向被撤销的原始操作的 seq，分配新的文档级全局 seq）。
- 将被撤销的操作从 undo 栈弹出并压入 redo 栈（保存完整操作快照，供 redo 时直接执行）。
- `import_sheet` 会清空 undo 栈，之后无法执行 undo。

**提交后失败策略**：当前实现以底层单元格写入成功（`applySetCell()` 成功返回）作为 undo 已提交的边界。此后若 history、user-op-state、audit 或广播失败，服务端只记录内部错误，不会再补发 `500`，客户端仍以已收到的 `undo_applied` 为准。

#### 完整发送示例

```json
{
  "type": "undo",
  "docId": "doc_sys_001",
  "clientId": "user_001"
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "undo" |
| docId | string | 是 | 文档 ID，必须是非空字符串，否则返回 4000 |
| clientId | string | 是 | 操作用户 ID，必须是非空字符串，否则返回 4000 |

**响应 — `undo_applied`（`broadcastToRoom` 广播给房间内所有客户端；发送方额外通过 `reply` 多收 1 条，共收到 2 条）**

```json
{
  "type": "undo_applied",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 2,
    "row": 1,
    "col": 1,
    "value": "",
    "style": null,
    "canUndo": false,
    "canRedo": true
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| seq | number | 本次 undo 操作分配的新序列号 |
| row | number | 被撤销的单元格行号 |
| col | number | 被撤销的单元格列号 |
| value | string | 撤销后该单元格的值（即执行 set_cell 之前的旧值） |
| style | object \| null | 撤销后该单元格的样式 |
| canUndo | boolean | 撤销后 undo 栈是否还有内容 |
| canRedo | boolean | 撤销后 redo 栈是否有内容（执行完 undo 后恒为 true） |

#### 错误响应示例

undo 栈为空（没有可撤销的操作）：

```json
{
  "type": "error",
  "code": 4000,
  "message": "nothing to undo",
  "data": null
}
```

缺少必填字段：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId and clientId are required",
  "data": null
}
```

未 join 该文档，或 socket 当前身份与 `clientId` 不一致：

```json
{
  "type": "error",
  "code": 4003,
  "message": "socket has not joined this document as the specified client",
  "data": null
}
```

undo 栈非空但文档已不存在：
- undo 栈为空时先触发 4000 nothing to undo，不会访问文档
- undo 栈非空时才会访问文档，文档不存在则返回 4004

```json
{ "type": "error", "code": 4004, "message": "document not found: doc_xxx", "data": null }
```

---

### redo — 重做操作

重做当前用户最近一次被撤销的操作。发送方必须已通过 `join` 加入该文档，且当前 socket 绑定的 `clientId` 必须与消息中的 `clientId` 一致，否则返回 `4003`。操作成功后：
- 先通过 `reply` 向发送方返回 `redo_applied`，再通过 `broadcastToRoom` 向房间内所有客户端（含发送方）广播同一条消息。**发送方会收到 2 条相同的 `redo_applied`**，其他客户端收到 1 条。
- 写入操作历史（opType: `"redo"`，sourceSeq 指向被重做的原始操作的 seq，分配新的文档级全局 seq）。
- 将被重做的操作从 redo 栈弹出并重新压入 undo 栈。
- 执行新的 `set_cell` 或 `import_sheet` 会清空 redo 栈，此后 redo 将返回 "nothing to redo"。

**提交后失败策略**：当前实现以底层单元格写入成功（`applySetCell()` 成功返回）作为 redo 已提交的边界。此后若 history、user-op-state、audit 或广播失败，服务端只记录内部错误，不会再补发 `500`，客户端仍以已收到的 `redo_applied` 为准。

#### 完整发送示例

```json
{
  "type": "redo",
  "docId": "doc_sys_001",
  "clientId": "user_001"
}
```

#### 发送字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 "redo" |
| docId | string | 是 | 文档 ID，必须是非空字符串，否则返回 4000 |
| clientId | string | 是 | 操作用户 ID，必须是非空字符串，否则返回 4000 |

**响应 — `redo_applied`（`broadcastToRoom` 广播给房间内所有客户端；发送方额外通过 `reply` 多收 1 条，共收到 2 条）**

```json
{
  "type": "redo_applied",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "seq": 3,
    "row": 1,
    "col": 1,
    "value": "姓名",
    "style": { "bold": true },
    "canUndo": true,
    "canRedo": false
  }
}
```

**响应 `data` 字段说明**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| seq | number | 本次 redo 操作分配的新序列号 |
| row | number | 被重做的单元格行号 |
| col | number | 被重做的单元格列号 |
| value | string | 重做后该单元格的值（即执行 set_cell 时的新值） |
| style | object \| null | 重做后该单元格的样式 |
| canUndo | boolean | 重做后 undo 栈是否有内容（执行完 redo 后恒为 true） |
| canRedo | boolean | 重做后 redo 栈是否还有内容 |

#### 错误响应示例

redo 栈为空：

```json
{
  "type": "error",
  "code": 4000,
  "message": "nothing to redo",
  "data": null
}
```

缺少必填字段：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId and clientId are required",
  "data": null
}
```

未 join 该文档，或 socket 当前身份与 `clientId` 不一致：

```json
{
  "type": "error",
  "code": 4003,
  "message": "socket has not joined this document as the specified client",
  "data": null
}
```

redo 栈非空但文档已不存在（与 undo 对称）：
- redo 栈为空时先触发 4000 nothing to redo，不会访问文档
- redo 栈非空时才会访问文档，文档不存在则返回 4004

```json
{ "type": "error", "code": 4004, "message": "document not found: doc_xxx", "data": null }
```

---

### 错误消息

以下情况在进入消息类型分发之前，服务端会直接返回错误：

#### 发送非法 JSON

输入任何无法解析的字符串，例如 { 或 hello。

响应：

```json
{
  "type": "error",
  "code": 4000,
  "message": "Invalid JSON message",
  "data": null
}
```

#### 发送非对象消息（如数组）

```json
[1, 2, 3]
```

响应：

```json
{
  "type": "error",
  "code": 4000,
  "message": "Invalid WebSocket message",
  "data": null
}
```

#### 发送不支持的消息类型

```json
{
  "type": "unknown_type"
}
```

响应：

```json
{
  "type": "error",
  "code": 4001,
  "message": "Unsupported message type",
  "data": null
}
```

---

## 附录

### 快照结构

snapshot 描述文档某一时刻的全量内容：

```json
{
  "id": "sheet_20260527_001",
  "name": "2026年销售数据表",
  "defaultRowHeight": 25,
  "defaultColWidth": 100,
  "rowCount": 100,
  "colCount": 26,
  "styles": {
    "style_header": {
      "fontFamily": "微软雅黑",
      "fontSize": 14,
      "bold": true,
      "color": "#FFFFFF",
      "bgColor": "#4472C4",
      "hAlign": "center"
    },
    "style_currency": {
      "fontFamily": "Arial",
      "fontSize": 12,
      "bold": false,
      "hAlign": "right"
    }
  },
  "cells": {
    "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
    "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
    "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
  }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 工作表 ID |
| `name` | string | 工作表名称 |
| `defaultRowHeight` | number | 默认行高 |
| `defaultColWidth` | number | 默认列宽 |
| rowCount | number | 表格行数 |
| colCount | number | 表格列数 |
| `styles` | object | 样式字典，键为样式 ID，值为样式对象；由 import_sheet 传入时原样保留 |
| styles[styleId].fontFamily | string | 字体名称，例如 `"微软雅黑"` |
| styles[styleId].fontSize | number | 字号 |
| styles[styleId].bold | boolean | 是否加粗 |
| styles[styleId].color | string | 文字颜色，通常为十六进制颜色值 |
| styles[styleId].bgColor | string | 背景颜色，通常为十六进制颜色值 |
| styles[styleId].hAlign | string | 水平对齐方式，例如 `"left"`、`"center"`、`"right"` |
| `cells` | object | 键为 `"行号:列号"`（如 `"0:0"`），行列均从 `0` 开始。注意：即使单元格值为空字符串，快照中也可能保留对应 key |
| cells[key].row | number | 单元格行号，从 `0` 开始 |
| cells[key].col | number | 单元格列号，从 `0` 开始 |
| cells[key].value | string | 单元格文本值 |
| cells[key].styleId | string \| null | 单元格关联的样式 ID，通常引用 `styles` 中的某个 key；无样式时为 `null` |

### 用户对象

在线用户列表（users 数组）中每个元素的完整结构：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | number | 内部自增主键 |
| docId | string | 所属文档 ID |
| clientId | string | 用户唯一标识 |
| name | string | 显示名称；未传时为 "" |
| color | string | 用户标识色；未传时为 "#3b82f6" |
| status | string | 在线状态，当前只返回 "online" 的用户 |
| joinedAt | string | 首次加入时间，ISO 8601 格式 |
| `lastActiveAt` | string | 与 `joinedAt` 相同；当前实现不会在后续操作中更新此字段，它始终等于该用户首次 join 时刻 |

### 种子数据

服务启动后自动写入以下文档，可直接用于测试：

| docId | title | createdBy | 内容 |
| --- | --- | --- | --- |
| doc_sys_001 | 2026年销售数据表 | system | 100 行 26 列，含表头样式和金额样式示例数据 |

doc_sys_001 的初始快照：

```json
{
  "id": "sheet_20260527_001",
  "name": "2026年销售数据表",
  "defaultRowHeight": 25,
  "defaultColWidth": 100,
  "rowCount": 100,
  "colCount": 26,
  "styles": {
    "style_header": {
      "fontFamily": "微软雅黑",
      "fontSize": 14,
      "bold": true,
      "color": "#FFFFFF",
      "bgColor": "#4472C4",
      "hAlign": "center"
    },
    "style_currency": {
      "fontFamily": "Arial",
      "fontSize": 12,
      "bold": false,
      "hAlign": "right"
    }
  },
  "cells": {
    "0:0": { "row": 0, "col": 0, "value": "产品名称", "styleId": "style_header" },
    "0:1": { "row": 0, "col": 1, "value": "销售金额", "styleId": "style_header" },
    "1:1": { "row": 1, "col": 1, "value": "9999.00", "styleId": "style_currency" }
  }
}
```
