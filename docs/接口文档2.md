# 行列插删与 Cursor 接口文档
- 版本：v2
- 范围：包含行列插删相关 4 个 WebSocket 接口，以及 1 个 cursor 光标广播接口
- WebSocket 地址：`ws://localhost:3000`

---

## 目录

- [通用约定](#通用约定)
- [请求消息总览](#请求消息总览)
- [成功广播总览](#成功广播总览)
- [insert_row — 插入空白行](#insert_row--插入空白行)
- [delete_row — 删除行](#delete_row--删除行)
- [insert_col — 插入空白列](#insert_col--插入空白列)
- [delete_col — 删除列](#delete_col--删除列)
- [cursor — 光标广播中继](#cursor--光标广播中继)
- [错误消息](#错误消息)

---

## 通用约定

- 本文中的 5 个接口均为 WebSocket 消息，不提供 HTTP 版本。
- 行列插删 4 个接口都必须先通过 `join` 加入目标文档房间。
- 行列插删 4 个接口要求发送方当前 socket 绑定的 `clientId` 必须与消息中的 `clientId` 一致，否则返回 `4003`。
- 行列接口中的行列下标均为 `1-based`。
- 插入行列表示插入空白行列，不会自动创建新单元格。
- 删除行列会删除目标行列上的单元格，其余单元格按规则整体平移。
- 任意一次结构变更成功后，都会清空该文档下所有用户的后端 `undo/redo` 栈，因此成功响应中的 `canUndo` 和 `canRedo` 固定为 `false`。
- `cursor` 是纯广播中继消息，约定前端在已 `join` 同文档后发送；后端不存储、不分配 `seq`、不写 history、不写 audit，也不校验权限和 `row/col` 合法性。

### 成功消息通用格式

```json
{
  "type": "响应类型",
  "code": 0,
  "message": "ok",
  "data": {}
}
```

### 错误消息通用格式

```json
{
  "type": "error",
  "code": 4000,
  "message": "错误描述",
  "data": null
}
```

### 业务码

| 业务码 | 含义 |
| --- | --- |
| 0 | 成功 |
| 4000 | 参数错误 / 业务规则不满足 |
| 4003 | 未 join 文档或 `clientId` 与当前 socket 身份不匹配 |
| 4004 | 文档不存在 |
| 5000 | 服务器内部错误 |

---

## 请求消息总览

```json
{ "type": "insert_row", "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "row": 3 }
```

```json
{ "type": "delete_row", "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "row": 3 }
```

```json
{ "type": "insert_col", "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "col": 2 }
```

```json
{ "type": "delete_col", "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "col": 2 }
```

```json
{ "type": "cursor", "docId": "doc_abc123", "clientId": "client_xyz", "row": 5, "col": 3 }
```

---

## 成功广播总览

```json
{ "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "seq": 12, "row": 3, "canUndo": false, "canRedo": false }
```

```json
{ "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "seq": 13, "row": 3, "canUndo": false, "canRedo": false }
```

```json
{ "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "seq": 14, "col": 2, "canUndo": false, "canRedo": false }
```

```json
{ "docId": "doc_sys_001", "clientId": "user_001", "sheetId": "sheet_20260527_001", "seq": 15, "col": 2, "canUndo": false, "canRedo": false }
```

```json
{ "docId": "doc_abc123", "clientId": "client_xyz", "row": 5, "col": 3 }
```

对应消息类型分别为：

- `row_inserted`
- `row_deleted`
- `col_inserted`
- `col_deleted`
- `cursor_update`

---

## insert_row — 插入空白行

在指定工作表的目标行之前插入一行空白行。

### 请求消息

```json
{
  "type": "insert_row",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetId": "sheet_20260527_001",
  "row": 3
}
```

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 `insert_row` |
| docId | string | 是 | 文档 ID |
| clientId | string | 是 | 操作用户 ID |
| sheetId | string | 是 | 目标工作表 ID |
| row | number | 是 | 1-based 行号，允许范围 `1 <= row <= rowCount + 1` |

### 处理语义

- 在第 `row` 行之前插入一行空白行
- 所有 `cell.row >= row` 的单元格整体下移一行
- `rowCount += 1`

### 成功广播

消息类型：`row_inserted`

```json
{
  "type": "row_inserted",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "seq": 12,
    "row": 3,
    "canUndo": false,
    "canRedo": false
  }
}
```

### 广播字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 操作用户 ID |
| sheetId | string | 实际写入的工作表 ID |
| seq | number | 本次操作的序列号 |
| row | number | 实际插入位置的 1-based 行号 |
| canUndo | boolean | 恒为 false |
| canRedo | boolean | 恒为 false |

---

## delete_row — 删除行

删除指定工作表中的目标行。

### 请求消息

```json
{
  "type": "delete_row",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetId": "sheet_20260527_001",
  "row": 3
}
```

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 `delete_row` |
| docId | string | 是 | 文档 ID |
| clientId | string | 是 | 操作用户 ID |
| sheetId | string | 是 | 目标工作表 ID |
| row | number | 是 | 1-based 行号，允许范围 `1 <= row <= rowCount` |

### 处理语义

- 删除第 `row` 行
- 目标行上的单元格被删除
- 所有 `cell.row > row` 的单元格整体上移一行
- `rowCount -= 1`

### 成功广播

消息类型：`row_deleted`

```json
{
  "type": "row_deleted",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "seq": 13,
    "row": 3,
    "canUndo": false,
    "canRedo": false
  }
}
```

### 广播字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 操作用户 ID |
| sheetId | string | 实际写入的工作表 ID |
| seq | number | 本次操作的序列号 |
| row | number | 实际删除的 1-based 行号 |
| canUndo | boolean | 恒为 false |
| canRedo | boolean | 恒为 false |

---

## insert_col — 插入空白列

在指定工作表的目标列之前插入一列空白列。

### 请求消息

```json
{
  "type": "insert_col",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetId": "sheet_20260527_001",
  "col": 2
}
```

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 `insert_col` |
| docId | string | 是 | 文档 ID |
| clientId | string | 是 | 操作用户 ID |
| sheetId | string | 是 | 目标工作表 ID |
| col | number | 是 | 1-based 列号，允许范围 `1 <= col <= colCount + 1` |

### 处理语义

- 在第 `col` 列之前插入一列空白列
- 所有 `cell.col >= col` 的单元格整体右移一列
- `colCount += 1`

### 成功广播

消息类型：`col_inserted`

```json
{
  "type": "col_inserted",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "seq": 14,
    "col": 2,
    "canUndo": false,
    "canRedo": false
  }
}
```

### 广播字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 操作用户 ID |
| sheetId | string | 实际写入的工作表 ID |
| seq | number | 本次操作的序列号 |
| col | number | 实际插入位置的 1-based 列号 |
| canUndo | boolean | 恒为 false |
| canRedo | boolean | 恒为 false |

---

## delete_col — 删除列

删除指定工作表中的目标列。

### 请求消息

```json
{
  "type": "delete_col",
  "docId": "doc_sys_001",
  "clientId": "user_001",
  "sheetId": "sheet_20260527_001",
  "col": 2
}
```

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 `delete_col` |
| docId | string | 是 | 文档 ID |
| clientId | string | 是 | 操作用户 ID |
| sheetId | string | 是 | 目标工作表 ID |
| col | number | 是 | 1-based 列号，允许范围 `1 <= col <= colCount` |

### 处理语义

- 删除第 `col` 列
- 目标列上的单元格被删除
- 所有 `cell.col > col` 的单元格整体左移一列
- `colCount -= 1`

### 成功广播

消息类型：`col_deleted`

```json
{
  "type": "col_deleted",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_sys_001",
    "clientId": "user_001",
    "sheetId": "sheet_20260527_001",
    "seq": 15,
    "col": 2,
    "canUndo": false,
    "canRedo": false
  }
}
```

### 广播字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 操作用户 ID |
| sheetId | string | 实际写入的工作表 ID |
| seq | number | 本次操作的序列号 |
| col | number | 实际删除的 1-based 列号 |
| canUndo | boolean | 恒为 false |
| canRedo | boolean | 恒为 false |

---

## cursor — 光标广播中继

将客户端光标位置直接广播给当前文档房间内的所有客户端。

### 请求消息

```json
{
  "type": "cursor",
  "docId": "doc_abc123",
  "clientId": "client_xyz",
  "row": 5,
  "col": 3
}
```

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | 固定值 `cursor` |
| docId | string | 是 | 文档 ID |
| clientId | string | 是 | 当前光标所属客户端 ID |
| row | number | 是 | 当前选中单元格行号，由前端保证合法 |
| col | number | 是 | 当前选中单元格列号，由前端保证合法 |

### 处理语义

- 不存储 cursor 状态
- 不分配 `seq`
- 不写 history
- 不写 audit 日志
- 不校验权限
- 不校验 `row/col` 合法性
- 直接执行 `broadcastToRoom(docId, cursor_update)`

### 成功广播

消息类型：`cursor_update`

```json
{
  "type": "cursor_update",
  "code": 0,
  "message": "ok",
  "data": {
    "docId": "doc_abc123",
    "clientId": "client_xyz",
    "row": 5,
    "col": 3
  }
}
```

### 广播字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| docId | string | 文档 ID |
| clientId | string | 当前光标所属客户端 ID |
| row | number | 当前选中单元格行号 |
| col | number | 当前选中单元格列号 |

---

## 错误消息

### 参数缺失

行操作：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId, clientId, sheetId and row are required",
  "data": null
}
```

列操作：

```json
{
  "type": "error",
  "code": 4000,
  "message": "docId, clientId, sheetId and col are required",
  "data": null
}
```

### cursor 说明

- `cursor` 消息没有单独的业务错误约束。
- 除非消息本身不是合法 JSON，或服务端出现内部异常，否则后端通常不会因为 `cursor` 请求返回专属错误。

### 工作表不存在

```json
{
  "type": "error",
  "code": 4000,
  "message": "sheet not found: sheet_missing_001",
  "data": {
    "docId": "doc_sys_001",
    "sheetId": "sheet_missing_001"
  }
}
```

### 未 join 或身份不匹配

```json
{
  "type": "error",
  "code": 4003,
  "message": "socket has not joined this document as the specified client",
  "data": null
}
```

### 范围越界

插入行：

```json
{
  "type": "error",
  "code": 4000,
  "message": "row must be between 1 and 101",
  "data": {
    "docId": "doc_sys_001",
    "sheetId": "sheet_20260527_001",
    "row": 102,
    "upperBound": 101
  }
}
```

删除行：

```json
{
  "type": "error",
  "code": 4000,
  "message": "row must be between 1 and 100",
  "data": {
    "docId": "doc_sys_001",
    "sheetId": "sheet_20260527_001",
    "row": 101,
    "upperBound": 100
  }
}
```

插入列：

```json
{
  "type": "error",
  "code": 4000,
  "message": "col must be between 1 and 27",
  "data": {
    "docId": "doc_sys_001",
    "sheetId": "sheet_20260527_001",
    "col": 28,
    "upperBound": 27
  }
}
```

删除列：

```json
{
  "type": "error",
  "code": 4000,
  "message": "col must be between 1 and 26",
  "data": {
    "docId": "doc_sys_001",
    "sheetId": "sheet_20260527_001",
    "col": 27,
    "upperBound": 26
  }
}
```
